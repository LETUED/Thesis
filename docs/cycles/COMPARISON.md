# 자율 사이클 하네스 A/B/C — v1·v2·v3 **실행** 비교

> 이 문서(cycle_3 브랜치본)는 **실제 실행** 3자 비교다. (refactor/master-plan 브랜치의 동명 문서는 v1↔v2 *시뮬레이션*이었고, 그 winner=v2 결론은 아래에서 실행으로 재평가됨.)

## 공통 기준
- **분기점 `993be5a` 동일** — 세 세대 모두 같은 코드 베이스에서 출발(공정 A/B/C).
- 하네스(.claude/commands/)만 v1/v2/v3로 교체. 각 회차 cy1~cy6 자율, cy6 독립평가(fresh-context/멀티렌즈).
- v3는 **회차=커밋**이라 `git log --grep='feat(cycle_3):'`로 사후 자동 검증 가능.

| 세대 | 브랜치 | 회차 | 커밋 구조 |
|---|---|---|---|
| v1 | refactor/master-plan | 15 | 회차별 15커밋 |
| v2 | cycle_2 | 15 | **squash 1커밋**(추적 0) |
| v3 | cycle_3 | 5(진행중) | **회차별 5커밋** |

## 핵심 대조표

| 축 | v1 (cycle_1) | v2 (cycle_2) | **v3 (cycle_3)** |
|---|---|---|---|
| 추적성 | 15커밋 | squash 1커밋 | **회차=커밋(git 자동검증)** |
| 정비(유지보수) | 5회(후반 **드리프트**) | **0회**(가치게이트가 회피) | **2회(정비 예산·연속2 캡)** |
| 드리프트 통제 | 없음(테스트 5연속) | 라벨 기반(신선도4·동선3 **못 막음**) | **surface 자동차단**(2연속서 전환) |
| 과대포장 | a11y 3회 분할 | aria-pressed **1줄을 회차화** | **최소산출 임계로 거부**(FinalCta 1줄) |
| 가치 검증 | 사후 자가진단 | **자가申告**(1줄도 "가치✅") | **git 실측 + 멀티렌즈** |
| 테스트 위조 | RSC 누출 "운으로" 잡음 | 약함 | **변이로 3회 적발**(01·04·05) |
| 위험클래스 | 단일평가 | (있으나 단일로 흐를 여지) | **멀티렌즈 강제 / 위험비례** |

## v3 실증 (cycle_3 5회차)

- 01 guest-regime-access(regime) → 02 guest-korea-indicators(regime) → 03 signup-value-props(signup) → 04 regime-engine-tests(backend) → 05 allocation-engine-tests(backend).
- **드리프트 차단**: 01·02 regime 2연속 → 03이 regime 수렴 막고 signup(미접촉)으로 전환(v2가 못 한 것).
- **정비 예산**: ② 갭 고갈 후 04·05 정비(cycle_2가 회피한 backend 엔진 커버리지 백필) — 연속 2 캡 도달, 06이면 ② 강제.
- **테스트 위조 감시**(cy6 변이 주입): 01 노출경계 동어반복·04 가짜 coverage 가드·05 한국디리스킹 0%커버를 각각 적발 → 실효 가드로 교체.
- **게이트**: frontend typecheck/22테스트/build 그린, backend pytest 73 그린.

## v3가 드러낸 한계 (dogfooding)

1. **드리프트룰이 "연속"만 본다** — regime이 2/5 빈도 편중인데 연속 끊기면(03) 통과. → **v4: "최근 N회 빈도 비율"로 확장** 필요.
2. **git 자동도출 untracked 누락** — 발견 즉시 하네스 보정(`322dff9`).
3. **위험비례 리뷰 vs 멀티렌즈 강제룰 긴장** — 검증된 재사용 컴포넌트는 단일 집중(02, 정당화 기록).

## 결론 — winner 재평가 (시뮬 → 실행)

- **기존(시뮬)**: `winner=v2, v2_prevents_drift=true`.
- **실행 검증**: v2는 드리프트를 **못 막았고**(신선도 4연속), 정비 0(회피), 자가申告, squash로 추적성 0이었다(cycle_2 독립 재평가). v3는 v1(드리프트·추적성)·v2(정비회피·과대포장·자가申告)의 결함을 **구조로 해소**함을 5회차로 실증.
- **정정**: `winner=v3 (실행 실증)`. 단 `v3_drift_rule = "연속 한정"` 한계 → **v4 과제(빈도 비율 드리프트룰)**.
- 부수 효과: v3는 v2가 회피한 백엔드 엔진 커버리지(regime·allocation 핵심 로직 — 경계·매도금지·과신가드·tier게이팅·한국디리스킹)를 실제로 백필.

> 회차 상세: `cycle_3/INDEX.md` + `cycle_3/01~05`. 하네스 설계·선행연구: `HARNESS_V3.md`.
