[사이클 1/6 · 조사] 작업에 들어가기 전 현재 상태를 감지한다.

작업: $ARGUMENTS

확인할 것:
- git: 브랜치, 미커밋 변경(`git status`/`git diff`), 최근 커밋 흐름
- 관련 파일: 이 작업이 건드릴 영역의 현재 코드 — 동작 / 미동작 / 누락 구분
- 제약: CLAUDE.md 설계철학·임계값·플랜구조와 충돌하는 지점
- 깨진 곳: 참조 누락, 죽은 코드(orphan), 기능 후퇴(regression) 가능성
- 라이브러리 불확실: 건드릴 코드가 Next.js·FastAPI·Supabase·Stripe API에 의존하고 그 동작이 확실치 않으면 context7 MCP로 현재 문서를 확인해 '동작/미동작'을 정확히 판정한다(기억 의존 금지)

산출: 현황 요약 + 이번 작업의 범위 가설(무엇을 / 어디를 바꿀지). 아직 설계·구현하지 말 것.
