[사이클 2/6 · 검색] 새로 짜기 전에 재사용할 것을 찾는다.

작업: $ARGUMENTS

찾을 것:
- 재사용 가능한 기존 컴포넌트/lib/타입/패턴 (중복 구현 방지) — Explore 에이전트 활용
- 비슷한 기능의 선례 — 이 코드베이스에서 같은 문제를 어떻게 풀었나
- 외부 근거가 필요하면(시장·세금·규제 등) /deep-research 또는 웹 검색 — 출처 URL 필수
- 의존 관계 — 이걸 바꾸면 어디가 영향받나

산출: "새로 만들 것 vs 가져다 쓸 것" 구분 목록 + 참고 자료(링크).
