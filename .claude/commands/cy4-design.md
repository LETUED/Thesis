[사이클 4/6 · 설계] 구현 전략을 설계하고, 스스로 점검한 뒤 자동으로 진행한다.

작업: $ARGUMENTS

설계할 것(/plan 수준):
- 파일별 변경 계획 — 무엇을 어떻게 바꾸나
- 인터페이스/타입 — props, 함수 시그니처, API 스키마 (any 금지)
- 데이터 흐름 — 서버→클라이언트, 캐시, Free/Pro 분기
- 위험·트레이드오프와 대안
- 라이브러리 API: 새 설계가 Next.js·FastAPI·Supabase·Stripe 등에 의존하면 context7 MCP로 최신 문서를 페치해 실재하는 현재 API로 설계한다(버전·시그니처 환각 금지)

기계 게이트(사람 승인 대체): 계획을 스스로 비판한다 — 범위 벗어남, 제약(설계철학·임계값) 위반, 타입/엣지케이스 누락이 없는지. 문제가 있으면 고친 뒤, 멈추지 말고 진행(/cy5)으로 넘어간다.
주의: 자가비판은 보조 신호일 뿐이다(LLM 단독 self-correction은 신뢰성이 낮다 — arXiv 2310.01798). 진짜 검증은 cy5의 테스트·빌드(외부 오라클)에서 일어난다 — 설계가 미심쩍은 부분은 cy5에서 검증할 테스트를 함께 설계하라.
- **최소 산출 임계(v3)**: 설계 결과 순 프로덕션 변경이 '의미변화 없는 1줄/속성-only' 수준이면 독립 회차로 만들지 말고 같은 surface 인접 작업과 묶어 회차당 가치를 확보한다(미세보강 우려먹기 방지).

산출: 자가 점검까지 끝난 구현 계획.
