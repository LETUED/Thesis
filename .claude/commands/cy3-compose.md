[사이클 3/6 · 구성] 조사 + 검색을 종합해 작업을 구조화한다. [v2: 드리프트 감지 + 가치 앵커]

작업: $ARGUMENTS

★ 방향 앵커(먼저 — cycle.md 방향 게이트의 cy3 재확인):
- **사용자-가치 한 줄**: 이 작업이 진전시키는 사용자가치를 한 줄로 명시한다(② "부담없이 올 수 있는" / 설계철학). 못 대면 '유지보수'로 분류하고 그 사실을 명시 기록한다(숨기지 않는다).
- **드리프트 감지**: 직전 3사이클(`docs/cycles/cycle_<세대>/` 최신 회차 파일들)의 결을 확인한다. 같은 결(예: 전부 'backend test', 전부 'a11y')이 연속되면 경고하고, 결이 **다른** 후보 ≥2개를 제시한 뒤 택한다. 유지보수 연속은 2회까지 — 3회째는 사용자-가치 주제 강제.

정리할 것:
- 요구사항 확정 — 무엇을 만들고, 무엇은 안 만드는가(범위 경계)
- 파일/모듈 단위 분해 — 어떤 파일을 새로 만들고 어떤 파일을 수정하나
- 엣지케이스·실패 경로 — 데이터 null, 캐시 stale, Free/Pro 게이팅 등
- 제약 체크 — 설계철학·임계값·플랜구조에 맞는가

중복생성 가드(자율 루프의 핵심 실패모드): 기존 코드·주석·노트를 '새로 만들 스펙'으로 오인하지 말 것. cy2 검색 결과의 재사용 대상을 먼저 확인하고, 이미 있는 것은 다시 만들지 않는다(중복 클래스·컴포넌트 생성 금지).

산출: 구조화된 작업 명세 + **사용자-가치 1줄**(또는 '유지보수: <무엇>' 표기) + 드리프트 점검 결과. 아직 구현하지 말 것.
